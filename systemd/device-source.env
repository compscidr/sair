# /etc/sair/device-source.env
# Environment variables for sair-device-source

# gRPC listen port
DEVICE_SOURCE_PORT=8080

# Port of the real ADB server (must match sair-adb-server.service)
ADB_PORT=5038
